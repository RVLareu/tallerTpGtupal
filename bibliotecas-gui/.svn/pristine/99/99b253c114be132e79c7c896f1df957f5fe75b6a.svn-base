#include "mainwindow.h"

#include <QApplication>

int main(int argc, char *argv[])
{
	// Clase que contiene el loop principal
    QApplication a(argc, argv);
    // Instancio la vista diseñada en QtCreator
    MainWindow w;
    w.show();
    // Arranca el loop de la UI
    return a.exec();
}
